---
summary: "CLI reference for `omniagent docs` (search the live docs index)"
read_when:
  - You want to search the live OmniAgent docs from the terminal
title: "docs"
---

# `omniagent docs`

Search the live docs index.

```bash
omniagent docs browser extension
omniagent docs sandbox allowHostControl
```
