---
summary: "CLI reference for `omniagent uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `omniagent uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
omniagent uninstall
omniagent uninstall --all --yes
omniagent uninstall --dry-run
```
